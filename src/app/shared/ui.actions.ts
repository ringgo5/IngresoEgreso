import { createAction } from '@ngrx/store';

export const isLoading = createAction('[Ui component Component] isLoading');
export const stopLoading = createAction('[Ui component Component] stop Loading');